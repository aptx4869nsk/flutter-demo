// download
enum DownloadStatus {
  notDownloaded,
  fetchingDownload,
  downloading,
  downloaded,
}

// response error
enum ApiErrType { respErr, dioErr }

enum DialogType {
  forward,
  base,
}

enum LoginType {
  password,
  sms,
}

// language setup
enum RadioStyle {
  blue,
  white,
}



